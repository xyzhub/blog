{
  // ? 引入外部的tsconfig.json的配置
  "extends": ["../../tsconfig.json"],

  // ? 需要重写的规则
  "compilerOptions": {
  },

  // ? 包含的检验的文件
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.vue", "src/**/*.d.ts"]
}
